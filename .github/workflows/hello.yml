name: Hello Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello World!"

      - name: Print Date
        run: date

      - name: List Files
        run: ls -l
